<title>PAYMENTS LIST FILTER BY PAGINATION</title>

<h1>PAYMENTS_LIST_FILTER_BY_PAGINATION-TEST</h1>

<% if load_msg == "OK" %>
	<h4 style="margin-bottom: -18px;">DATA_RESPONSE</h4>
	<pre><%= obj_res.to_yaml %></pre>
	<% if obj_res['data']['itemReference'] != nil && obj_res['data']['itemReference'] != "" %>
		<a href="?itemReference=<%= obj_res['data']['itemReference'] %>">Next</a>
	<% end %>
<% else %>
	<pre><%= load_msg %></pre>
<% end %>

</br></br>---</br>
<a href="/">Back</a>